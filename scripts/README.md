Each script represents an independent analytical workflow.
No execution order is required, as all scripts operate on the same cleaned dataset and do not depend on intermediate outputs from other scripts.
**All scripts rely on the same cleaned dataset and shared preprocessing logic.**
